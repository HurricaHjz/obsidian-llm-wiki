# Wiki Log

> Append-only timeline. Append a `## [date] action | title` entry on every brain-updating op — **via
> shell (`cat >> wiki/log.md`), never by reading the whole file**. Read/grep it only on demand or for
> debugging. Actions: `ingest` · `query` · `lint` · `sync` · `setup` · `maps`.

## [2025-01-01] setup | Initialised from the LLM-Wiki framework template
- **Changed**: created the `raw/` + `wiki/` scaffold, the registries, and a small seed example.
- **Conflicts**: none.

## [2025-01-01] ingest | (example) GPT-4 and the MMLU benchmark
- **Changed**: created [[example-gpt4-and-mmlu]], [[Large Language Model]], [[OpenAI]], [[GPT]], [[MMLU]], [[home]]; updated [[index.md]].
- **Conflicts**: none.
