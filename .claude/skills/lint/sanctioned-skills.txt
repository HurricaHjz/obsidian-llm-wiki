# Sanctioned agent-skill directories (lint Step 2e — skill-injection guard).
# One entry per line: "vault:<name>" = <vault>/.claude/skills/<name>.
# A directory on disk but not listed here is a FINDING (third-party installers target these roots —
# e.g. Agent Reach's SKILL.md auto-install; see wiki/developments/agent-reach-adoption-design.md).
# The machine-local baseline for ~/.claude/skills lives OUTSIDE the vault at ~/.claude/skills/.sanctioned.txt (one name per line) — never shipped.
# Adding a skill is a conscious CLAUDE.md §12 act: add its line here in the same pass.
vault:attic
vault:deep-lint
vault:export-template
vault:gather
vault:ingest
vault:lint
vault:output
vault:qmd-search
vault:query
vault:reflect
