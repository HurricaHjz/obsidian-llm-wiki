# lane-home-src — the skill slices a headless lane carries

A headless lane sees only the skills in its lane home plus the user-level ones. This directory is
the **source** of the vault-owned slices: the wrapper copies the ones a class's `routing.json` row
names into that spawn's lane home under `.claude/skills/`, and the wrapper appends them to the lane's system prompt (`--append-system-prompt-file`),
the one carrier measured to reach a restricted lane; a `skills:` preload never loads there (2026-09-04).

A slice is a **hand-made subset** of a vault skill: the half a lane performs, with the half the
head performs left out. It is not generated, and nothing keeps it in step automatically.

| Slice | Source | Sections taken | Deliberately left out |
|---|---|---|---|
| `compile-core/SKILL.md` | the ingest skill | Depth (range, triggers, overrides, the locator rule) · Step 3 (both source-page templates, verbatim) · Step 4 (network duties) · Step 5 (registries, as propose-don't-write) · the claims schema from Parallel mode · Hard constraints | conversion and de-dup, pacing, parallel orchestration and the merge, the verify step, raw sorting, the run ledger's mechanics, the routing notes |
| `reflect-slice/SKILL.md` | the reflect skill | Steps 1–5 (register, horizon and sweep, the three-clause bar, routing, grading) · the reflector prefill's report shape and its named-discard rule | Step 2b (usage and waste — head-only: needs the meter and the run's records), Steps 5b–8 (the critic pass, the preview, writing, logging, the recommendations), `--yes` and every write path, the trigger rules |

Two neighbours in the lane home come from elsewhere and are not slices of a skill:

- `templates/lane-core.md` in the delegate skill — the block the wrapper appends once per spawn (a brief never pastes it), replacing
  `_inherited.md`. Its home is the delegate templates directory, because the spawner assembles it.
- `contract/schema-s4.md` and `contract/confidence-rubric.md` — extracts of the schema's page rules
  and of the confidence rubric, materialised beside the slices under names that do not auto-load,
  and granted by path on a verifier or compile reading list. They are extracts of the governing
  contract, so they are cut and installed by the head, never by a lane.

## Keeping a slice true

**A slice is regenerated by hand when its source changes.** The duty falls at the source's next
edit: whoever edits the ingest or reflect skill re-reads the slice in the same pass and carries the
change across, or records why it does not apply. The two are separate files by design — the lane
needs a fraction of the skill and the head needs all of it — so the cost of the split is exactly
this one duty.

Two properties make the duty checkable, and both are worth a deep-lint leg (**proposed, not built**:
no script here implements them):

- Every heading this README's table claims a slice takes still exists in the source skill, and the
  passage the slice carries still matches it. A source heading that has been renamed or removed is
  the signal that a slice went stale.
- The verbatim blocks — the two source-page templates in `compile-core` — are byte-identical to the
  source skill's copies. They are the one part where a paraphrase would silently change what a lane
  writes to disk, so they are copied, never re-typed.

Until that leg exists, the check is the editor's, and a slice that has drifted is a defect for the
register like any other.
